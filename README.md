# 大乃的知識星圖：2026 六至八月

## 目前版本：以工作與生活為中心

主圖改為 7 個重心、19 個知識關鍵字。初始只顯示 26 個節點；點重心或關鍵字，才展開相關專案。Claude 和其他工具退到「來源與工具」與專案明細，不再是圖上的中心。

七個重心：國文教學、學生學習與評量、自我領導與班級經營、資訊組與校務、教師成長與分享、閱讀寫作與創作、生活與理財。分類是可調整的用途詮釋，不是因果認證。生活面向目前主要有理財資料，沒有臆造家庭或健康紀錄。

原來 83 個來源索引節點、152 條原始連線、53 個具名專案與所有來源統計仍保留於匯出資料。新的主題、關鍵字、專案／實踐共 96 節點採分層顯示；工具不進主圖。

`reframe.mjs` 可將原始去識別資料轉換為新版：

```sh
node reframe.mjs /path/to/summer-data.json
node test-public.cjs
node test-physics.cjs
```

本機原匯入流程已接上這個轉換，重新匯入不會恢復 Claude 中心的舊畫法。

正式網站：https://danai-knowledge-summer-2026.netlify.app

可拖曳、彈性牽引、平移、縮放、按月／單日篩選與搜尋的靜態互動網頁。不依賴外部套件、不連接後端。

## 使用

以瀏覽器開啟 `public/index.html`，或部署 `public/`。拉住節點可牽引相連項目，放開後逐漸穩定。點重心／關鍵字逐層展開，點專案查看匿名來源。原始共現候選保留在匯出資料，不混入新版分類連線。

新版線上驗收：總覽 26 節點；「評量與回饋」可展開批改平台、教師規準及學生作品；六月／七月／八月總覽分別 17／24／26 節點。實際拖動「閱讀理解」會帶動「國文教學」，且拖曳不會誤觸展開。390px 手機沒有水平溢出。正式頁 sourceNodes／sourceLinks 與原始去識別資料逐項一致。

## 本次資料

- 2026/06/01–08/31，共 92 天。
- 1,097 份 Claude 工作文件內容已讀；1,503 份 Codex／每日筆記。
- 313 個 Claude 工作階段，4,666 筆互動索引，按工作階段與日期合併。
- 14,232 筆 Chrome Default 造訪，按日期／主題彙總。
- 83 個節點，其中 53 個是具名網站／專案線索。

以上是目前保留、已找到的來源，不是所有歷史操作的完整還原。Claude 工作階段來自 history 索引，並非逐個工作階段的完整工具執行逐字稿；未查其他身分未確認的 Chrome 設定檔。六至八月沒有電腦背景操作摘要。網站接觸、文件提及、提出工作要求，都不表示已完成專案。

## 隱私與發布範圍

`public/` 僅一個 HTML，內含主題、日期與匿名統計。不包含原始對話、筆記原文、文件標題、私人網址、帳號、本機路徑或私密索引。原始來源與匯入程式留在使用者本機，不放此儲存庫。

Netlify 設定禁止搜尋引擎索引、外部網路請求及嵌入；這不是登入保護，有網址的人仍可讀取公開版。公開內容限上述去識別資料。

## 驗證

```sh
node test-public.cjs
node test-physics.cjs
```

範本為 `template.html`；資料嵌入於 `public/index.html`。更新範本須重新產生 HTML。檢查包含日期範圍、資料對帳、敏感資訊、節點連線與完整網路的運動穩定性。

## 部署

以 Netlify CLI 部署 `public/`，設定在 `netlify.toml`。不部署工作目錄上層或私人來源。每日排程不在這次發布範圍。

## 首版正式驗收紀錄

2026-09-09：正式站 HTTP 200；匯入產物與正式頁內容一致，另有 Netlify 自動附加的 HUD 腳本標籤，受 CSP 阻擋。五個私人來源／程式路徑皆回傳 404。

瀏覽器實測預設 83 節點、52 條來源實線；開啟候選後 152 條。六月 32 節點、七月 61、八月 77。搜尋與鍵盤選取「文豪笑傳」可開啟來源明細。實際拖曳會牽引鄰接節點；390px 手機模擬視窗沒有水平溢出。

仍有 444 筆來源待細分類；共現候選 421 組只顯示前 100 組，這是探索介面，不是全面語意查核結果。
